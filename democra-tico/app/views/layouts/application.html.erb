<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Democra-Tico - Programathon 2017" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>

  </head>

  <body>
  <header>
    <div class="top-bar">
      <div class="top-bar-right">
        <ul class="menu">
          <%- if current_user %>
            <li class="has-sub-menu" role="menuitem"><%= link_to('Mi Cuenta |', edit_user_registration_path) %></li>
            <li role="menuitem"><%= link_to('Salir |', destroy_user_session_path, :method => :delete) %></li>
          <% if current_user.user_type == 'Administrador' %>
            <li role="menuitem"><%= link_to('Administrar Usuarios', users_path) %></li>
          <%- end %>
          <%- else %>
            <li role="menuitem"><%= link_to "Login", new_user_session_path %></li>
          <%- end %>
        </ul>
      </div>
      <div class="top-bar-left">
        <span style="color:white" class="site-title">DEMOCRA-TICO</span>
      </div>
    </div>
  </header>
    <div class="column">
      <main role="main">
        <span class="notice"><%= notice %></span>
        <span class="alert"><%= alert %></span>
        <%= yield %>
      </main>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>
